<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="message">
   
     <insert id="sendMessage" parameterType="Message">
        <![CDATA[INSERT INTO TB_MESSAGE(MESSAGE_NO, SENDER_ID, RECEIVER_ID, MESSAGE_CONTENT) VALUES (MESSAGE_SEQ.NEXTVAL, #{senderId}, #{receiverId}, #{messageContent})]]>
   	 </insert>
   	 
   	 <select id="selectSendMessage" resultType="Message" parameterType="string">
   	  	<![CDATA[SELECT * FROM TB_MESSAGE WHERE SENDER_ID = #{senderId}
   	  	]]>
   	 </select>
   	 
   	 <select id="selectReceiverMessage" resultType="Message">
   	 	<![CDATA[SELECT * FROM TB_MESSAGE
   	  			 WHERE RECEIVER_ID = #{user_id}
   	  	]]>
   	 </select>
   	 
</mapper>